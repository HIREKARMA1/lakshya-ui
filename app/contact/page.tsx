import { ContactPage } from "@/components/pages/contact-page";

export const metadata = {
  title: "Contact Us — LAKSHYA",
  description: "Get in touch with the LAKSHYA team — call, WhatsApp, or email for support.",
};

export default function ContactRoute() {
  return <ContactPage />;
}
